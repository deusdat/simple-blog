<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>{{.Title}}</title>
</head>
<body>
    {{ if not .Articles}}
        <h2>No blogs found</h2>
    {{ else }}
        {{ range .Articles }}
            <section>
                <h1>{{ .Title}}</h1>
                <div>{{.Content}}</div>
            </section>
        {{ end }}
    {{ end }}
</body>
</html>